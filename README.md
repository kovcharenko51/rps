
From main dir:

to install
```mkdir build && cd build && cmake .. && make && cd ..```

to run
```./bin/RPS```

to uninstall
```rm -rf build bin```

# Requires:
sfml(```sudo apt-get install libsfml-dev```)
